package com.example.tejpatel.distancecalculator;import android.content.Intent;import android.support.v7.app.AppCompatActivity;import android.os.Bundle;import android.util.Log;import android.view.View;import android.widget.Button;import android.widget.EditText;import android.widget.RadioButton;import android.widget.TextView;public class MainActivity extends AppCompatActivity {    double result;    final double milesMultiplier = 1.61;    final double kilometersMultiplier = 0.62;    EditText amountEditText;    RadioButton milesRadioButton;    RadioButton kilometersRadioButton;    TextView resultAmountTextView;    TextView resultUnitTextView;    @Override    protected void onCreate(Bundle savedInstanceState) {        super.onCreate(savedInstanceState);        setContentView(R.layout.activity_main);        // Input UI element instantiation        amountEditText = (EditText) findViewById(R.id.amount_edit_text);        milesRadioButton = (RadioButton) findViewById(R.id.miles_radio_button);        kilometersRadioButton = (RadioButton) findViewById(R.id.kilometers_radio_button);        Button calculateButton = (Button) findViewById(R.id.calculate_button);        // Result UI element instantiation        resultAmountTextView = (TextView) findViewById(R.id.result_amount_text_view);        resultUnitTextView = (TextView) findViewById(R.id.result_unit_text_view);        // On Click Listener for the calculate button        calculateButton.setOnClickListener(new View.OnClickListener() {            @Override            public void onClick(View v) {                int amount = Integer.parseInt(amountEditText.getText().toString());                calculate(amount);            }        });    }    public void calculate(int inputAmount) {        if (milesRadioButton.isChecked()) {            result = inputAmount * milesMultiplier;            resultAmountTextView.setText(Double.toString(result));            resultUnitTextView.setText("Kilometers");        } else if (kilometersRadioButton.isChecked()) {            result = inputAmount * kilometersMultiplier;            resultAmountTextView.setText(Double.toString(result));            resultUnitTextView.setText("Miles");        }    }}